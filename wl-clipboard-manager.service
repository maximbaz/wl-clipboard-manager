[Unit]
Description=Clipboard manager daemon
PartOf=graphical-session.target
After=graphical-session.target

[Service]
ExecStart=/usr/bin/wl-clipboard-manager daemon
Restart=always

[Install]
WantedBy=graphical-session.target
